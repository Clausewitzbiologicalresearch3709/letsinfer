# Let's Infer standard context fixture

Fixture: `{{FIXTURE_ID}}`.

The exact retrieval marker is `{{MARKER}}`. Preserve it precisely.

--- BEGIN IMMUTABLE CONTEXT ---

{{BODY}}

--- END IMMUTABLE CONTEXT ---

Summarize the central evidence in the context, explain the strongest supported
inference, and finish by reproducing the exact retrieval marker. Be precise
and rely only on the supplied context.
