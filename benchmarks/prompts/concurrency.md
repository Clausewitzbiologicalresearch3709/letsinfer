# Let's Infer standard concurrent fixture

Fixture: `{{FIXTURE_ID}}`. Client slot: `{{SLOT}}`.

The exact retrieval marker is `{{MARKER}}`. Preserve it precisely.

--- BEGIN IMMUTABLE CONTEXT ---

{{BODY}}

--- END IMMUTABLE CONTEXT ---

Identify the central claim and two supporting details, distinguish observation
from inference, and finish with the exact retrieval marker and client slot.
Use only the supplied context.
